# CLAUDE.md

מדריך ל-AI assistants (ובני אדם) שעובדים על הריפו הזה.

> השפה הראשית של הריפו היא **עברית** — תיעוד, הערות, ומחרוזות פלט בעברית; קוד
> ומזהים באנגלית. שמור על כך.

---

## מה זה הריפו

`factum-il-stage-a` — **שלב א'** של שכבת הרב-סוכנים של Factum IL: הוכחת היתכנות
(POC) בסיכון מינימלי שמדגימה מקצה-לקצה שלושה דברים — תכנון/ניתוב, ביצוע דרך זיכרון
משותף, וממשל אכוף בקוד. אם השלב הזה עובד, כל השאר הוא הרחבה של אותה תשתית.

**עיקרון מנחה:** אפס תלות חיצונית — הכול רץ מקומית על Node.js (חיסיון). אין npm
install, אין רשת, אין SDK.

---

## מבנה

```
package.json                  # מטא-דאטה + סקריפטים (ללא dependencies)
stage-a/
  README.md                   # אפיון מפורט של השלב + תוצאת הרצה צפויה
  run-stage-a.js              # נקודת הכניסה — מריץ את ה-POC מקצה לקצה
  src/
    shared-memory.js          # SharedMemory — אפיק append-only, אוכף evidence[]
    manager.js                # Manager — מתכנן ומנתב בלבד (לא מבצע)
    worker-summarizer.js      # SummarizerWorker — סיכום מעוגן-מקור
```

---

## הרצה

```bash
npm start          # = node stage-a/run-stage-a.js   (מהשורש)
npm run stage-a    # זהה
```

> שים לב: ה-`README.md` מציין `node run-stage-a.js`, אך הסקריפטים ב-`package.json`
> רצים מהשורש (`node stage-a/run-stage-a.js`). העדף את סקריפטי ה-npm.

ההרצה מדפיסה 5 שלבים: תכנון · ביצוע · הרכבת פלט סופי · היומן המלא · אימות ממשל.
אין צורך ב-`npm install` (אין תלויות). אין test runner — האימות מובנה בריצה עצמה
(`verifyGovernance` בסוף `run-stage-a.js`), ומחזיר `process.exitCode = 1` אם מנגנון
ממשל כלשהו אינו נאכף.

---

## הרכיבים והחוזים

- **`SharedMemory`** — single-source-of-truth שדרכו המנהל והעובדים מתקשרים.
  append-only: כל רשומה מוקפאת (`Object.freeze`), אין `delete`/`update`. רשומת
  `worker-output` **חייבת** `evidence[]` לא-ריק (נאכף ב-`append`). שדות רשומה:
  `entryId, taskId, author, stepId, kind, payload, evidence, ts`.
- **`Manager`** — מקבל מטרה ומפרק לתוכנית (`taskId, goal, steps[], maxSteps`),
  כותב אותה לזיכרון, ומנתב צעדים מוכנים (`nextSteps`, לפי `dependsOn[]`). **אינו
  מבצע תת-משימות** ואין לו חובת evidence (הוא מנתב, לא מבסס טענות). אוכף תקרת
  צעדים (`maxSteps`, ברירת מחדל 5).
- **`SummarizerWorker`** — עובד יחיד בשלב זה. סיכום מעוגן-מקור: כל נקודה מצרפת
  `evidence` המפנה לשורת מקור; אסור להמציא תוכן; קלט חסר → תוצר `partial` עם דגל
  `incomplete-source` (עצירה בטוחה לסקירת אדם). אינו מודע למנהל או לעובדים אחרים —
  מקבל צעד + זיכרון בלבד. `inputRef` תומך ב-`source:<id>` ו-`step:<id>`.

---

## חמשת מנגנוני הממשל (נאכפים בקוד, לא רק מתועדים)

1. `evidence[]` חובה על תוצר עובד.
2. תקרת צעדים עוצרת לולאות בורחות.
3. עובד עוצר-בטוח על קלט חסר (`partial` + דגל).
4. הזיכרון append-only (ביקורת מלאה, רשומות מוקפאות).
5. פלט חלקי מסומן לסקירת אדם.

כל בדיקה ב-`verifyGovernance` **מנסה להפר** את המנגנון ומוודאת שההפרה נחסמת. אם
אתה משנה רכיב, ודא שכל החמישה ממשיכים לעבור (`ENFORCED`).

---

## קונבנציות

- **Node.js / CommonJS** (`require`/`module.exports`), `'use strict'` בכל קובץ.
- **אפס תלויות** — אל תוסיף חבילות npm. שמור על POC מקומי בלבד.
- הערות ומחרוזות פלט בעברית; שמות מחלקות/פונקציות/משתנים באנגלית.
- שמור על העקרונות: ביסוס (evidence), עצירה בטוחה, שקיפות (יומן מלא), חיסיון
  (מקומי בלבד), אדם-בפיקוד (פלט partial לסקירה).

---

## הצעד הבא — שלב ב' (הרחבה, לא ארכיטקטורה חדשה)

המבנה כבר תומך בהרחבה ללא שינוי ארכיטקטוני:

- **ריבוי עובדים** — מילון ה-`workers` ב-`run-stage-a.js` כבר מאפשר רישום של
  עובדים נוספים (Timeline-Builder, Contract-Review) לצד ה-Summarizer.
- **תלות בין צעדים** — `dependsOn[]` ו-`_resolveInput` (`step:<id>`) כבר קיימים.
- **סוכן סוקר** — שלב ג' יוסיף Reviewer שבוחן את המכלול לפני מסירה.

---

## עבודה עם Git

- פתח/דחוף לענף הפיתוח הייעודי; אל תדחוף ל-`main` בלי אישור מפורש.
- קומיטים תיאוריים (הקיימים בעברית — שמור על הסגנון).
