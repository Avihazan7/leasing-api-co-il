# <Agent-Name> — מפרט סוכן (AGENT.md)

> תבנית ריקה ליצירת סוכן חדש. העתק קובץ זה אל `agents/<name>.AGENT.md`, מלא את שבעת המקטעים, ודא שכל חמשת עקרונות-העל מיושמים — במיוחד שדה ה-`evidence[]` — ורשום את הסוכן ב-`AGENTS.md`.
>
> הפעל את הסקיל `agent-architect` ("תגדיר לי סוכן ש...") כדי שינחה אותך דרך המילוי.

---

## 1. זהות

| שדה | ערך |
| --- | --- |
| `id` | מזהה קצר באנגלית, kebab-case (למשל `summarizer`). זהה למפתח שבו נרשם העובד בקוד. |
| שם תצוגה | שם קריא לבני אדם. |
| פרופיל | `legal` (Factum) או `commercial` (Leasing). הפרופיל קובע את ניסוח עקרונות-העל. |
| גרסה | semver. כל שינוי בחוזה הפלט מעלה גרסה. |

## 2. תחום אחריות

מה הסוכן עושה — במשפט אחד — ומה הוא **אינו** עושה. גבול האחריות מפורש: סוכן עובד מבצע תת-משימה אחת, אינו מתכנן (זה תפקיד המנהל) ואינו מודע לעובדים אחרים. רשום מפורשות את הדברים שמחוץ לתחום כדי למנוע זחילת אחריות.

## 3. חוזה קלט

- **`inputRef`** — צורת ההפניה לקלט: `source:<id>` (מקור גולמי) או `step:<id>` (תוצר של צעד קודם בזיכרון המשותף).
- **מבנה הקלט הצפוי** — הסכימה שהסוכן יודע לקרוא.
- **תלות** — אילו צעדים קודמים חייבים להסתיים (`dependsOn[]`) לפני שצעד זה רץ.

## 4. חוזה פלט

הסוכן כותב לזיכרון המשותף רשומת `worker-output` בלבד. מבנה ה-`payload`:

```jsonc
{
  "status": "complete" | "partial",   // partial → מיועד לסקירת אדם
  "flags": [],                          // למשל "incomplete-source"
  "<תוכן הסוכן>": ...                    // למשל summary[] / timeline[] / score
}
```

**`evidence[]` — חובה ולא-ריק על כל תוצר עובד.** כל טענה שהסוכן מפיק מצורפת לעוגן שמפנה למקור (`{ kind, ref, quote }`). הזיכרון המשותף דוחה תוצר עובד ללא `evidence`. זוהי נקודת-האכיפה המרכזית של עקרון הביסוס.

## 5. כללי התנהגות

הכללים הקונקרטיים שהסוכן מציית להם. לכל הפחות:

- כל פלט מעוגן במקור; **אין להמציא תוכן** שאינו קיים בקלט.
- דטרמיניזם היכן שניתן — אותו קלט מפיק אותו פלט.
- הסוכן אינו פונה לרשתות חיצוניות (חיסיון — ראו מקטע 6).

## 6. עקרונות-העל (חמישה)

כל סוכן מיישם את חמשת העקרונות. נסח כל אחד בהקשר הסוכן הספציפי:

| עיקרון | כיצד נאכף בסוכן זה |
| --- | --- |
| **ביסוס** | `evidence[]` חובה על כל תוצר; כל טענה מפנה לשורת מקור. |
| **עצירה בטוחה** | קלט חסר/לא-תקין → תוצר `partial` עם דגל, לא קריסה. ראו מקטע 7. |
| **שקיפות** | כל פעולה נכתבת לזיכרון המשותף append-only; שום צעד אינו סמוי. |
| **חיסיון** | אפס תלות חיצונית; כל העיבוד מקומי; אין הדלפת מקור. |
| **אדם-בפיקוד** | תוצר `partial` ו/או כל התחייבות מהותית מיועדים לאישור אדם. |

## 7. עצירה בטוחה ושער אנושי

- **מצבי כשל** — מנה את התרחישים (קלט חסר, סכימה לא תואמת, חריגה מהיקף) וכיצד כל אחד מתורגם ל-`status: "partial"` + דגל מתאים.
- **שער אנושי** — מתי הסוכן **חייב** לעצור ולהעביר לאדם (פרופיל מסחרי: כל התחייבות כספית/חוזית; פרופיל משפטי: כל מסקנה משפטית מהותית).
- **תקרת צעדים** — הסוכן פועל בתוך תקרת הצעדים שהמנהל אוכף; אינו מזמן את עצמו בלולאה.

---

> **דוגמה לשלב הבא — Verification-Agent (תוכנית V14):** סוכן שמקבל תוצר של סוכן אחר ובודק כל `evidence.ref` מול המקור המקורי, מסמן כל טענה כ-`verified` / `unverified`, וכל טענה לא-מאומתת מורמת אוטומטית לשער אנושי. הוא ממחיש את כל שבעת המקטעים: קלט = `step:<id>`, פלט = דוח אימות עם `evidence[]` כפול (העוגן המקורי + הבדיקה), ועצירה בטוחה כש-`ref` אינו נפתר.
