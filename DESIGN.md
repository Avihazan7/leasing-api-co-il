# DESIGN — שפת הממשק · Factum IL

> ממשל חזותי: מסמך מקור אחד שמגדיר כיצד כל מסך נראה. כל טוקן מחזיק ערך, כלל, וה"למה" מאחוריו, וניתן לאכוף ולבדוק. מנחה את ה-React 19 Dashboard.
>
> פרופיל: **`legal`** — בהירות לפני קישוט, עדות גלויה לצד כל טענה, ושום מסקנה אינה מוצגת בלי המקור שלה.

## עקרונות

1. **עדות גלויה** — כל טענה שמקורה בסוכן מוצגת לצד העוגן שלה (ציטוט המקור). אין "תוצאה" ללא מקור נראה במרחק קליק.
2. **מצב partial גלוי** — תוצר שדורש סקירת אדם מסומן חזותית (תג אזהרה), לעולם אינו מוצג כמושלם.
3. **שקיפות תהליך** — היומן append-only נגיש מכל מסך; המשתמש תמיד יכול לראות מי עשה מה ומתי.
4. **בהירות לפני קישוט** — צפיפות מידע משפטי גבוהה; ויזואליזציה משרתת הבנה, לא רושם.

## טוקני צבע

| טוקן | ערך | שימוש / "למה" |
| --- | --- | --- |
| `--color-bg` | `#0f1115` | רקע כהה — קריאה ממושכת של מסמכים. |
| `--color-surface` | `#171a21` | כרטיסים ופאנלים. |
| `--color-text` | `#e6e8ec` | טקסט ראשי, ניגודיות AA+. |
| `--color-evidence` | `#3b82f6` | עוגני מקור — הצבע ש"מבטיח" שיש מקור. |
| `--color-warning` | `#f59e0b` | תג `partial` / נדרשת סקירת אדם. |
| `--color-verified` | `#22c55e` | טענה שאומתה מול המקור. |
| `--color-danger` | `#ef4444` | סיכון/החרגה שזוהתה ע"י Contract-Review. |

## טוקני טיפוגרפיה ומרווח

| טוקן | ערך | "למה" |
| --- | --- | --- |
| `--font-ui` | `Inter, "Heebo", sans-serif` | תמיכת RTL מלאה לעברית. |
| `--font-mono` | `"JetBrains Mono", monospace` | ציטוטי מקור ומזהי-עוגן. |
| `--space-unit` | `4px` | כל המרווחים כפולות של היחידה. |
| `--radius` | `8px` | פינות כרטיסים. |

## דפוסי רכיבים

- **EvidenceChip** — תג קטן בצבע `--color-evidence` שמציג `ref` ובלחיצה פותח את ציטוט המקור (`quote`). מופיע לצד כל נקודת סיכום / שורת ציר-זמן.
- **PartialBanner** — באנר ב-`--color-warning` בראש כל תוצר שסטטוסו `partial`, עם הדגל (למשל `incomplete-source`) ופעולת "העבר לסקירה".
- **AuditDrawer** — מגירה צדדית שמציגה את היומן append-only של המשימה הנוכחית.
- **TimelineRail** — ציר אנכי לתוצרי Timeline-Builder; כל אירוע נושא EvidenceChip.

## כלל אכיפה

רכיב שמציג תוצר סוכן **חייב** לרנדר את ה-`evidence` שלו. תוצר ללא `evidence` הוא באג ממשק — בדיוק כפי שהזיכרון המשותף דוחה אותו בצד הלוגיקה. הממשל החזותי והממשל ההתנהגותי אוכפים את אותו עיקרון משני צידי המסך.
