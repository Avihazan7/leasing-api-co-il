# DESIGN.md — Factum IL

> מסמך מקור יחיד לשפת ה-UI של Factum IL. כל סוכן קוד (וכל מפתח אנושי) קורא קובץ זה לפני בניית מסך, ומפיק ממנו ממשק עקבי. הקובץ מחזיק את הטוקן, את הכלל, ואת ה**למה** מאחוריו — כך שגם במקרה שאינו מכוסה במפורש, ההחלטה הבאה תישאר על-שיטה.

---

## 1. Visual Theme — תמה חזותית

**Forensic Authority** — סמכות משפטית רגועה.

Factum IL היא מערכת שעורך דין סומך עליה בהליך משפטי. הממשק חייב לשדר שלושה דברים בו-זמנית: דיוק (כל מספר, כל ציטוט נכון), שקיפות (תמיד רואים מאיפה הגיע מידע), ושליטה (המשתמש בפיקוד, לא ה-AI). האסתטיקה היא **editorial-technical**: צפיפות מבוקרת של מידע, היררכיה טיפוגרפית חדה, וצבע מינימלי המשמש כסימון משמעות ולא כקישוט.

זוהי מערכת לעבודה ממושכת. היא נוטה ל-**theme בהיר כברירת מחדל** לקריאת מסמכים ממושכת, עם theme כהה זמין לעבודת לילה ולמרכז הפעולות. הרושם הנכון: מכשור משפטי מקצועי — לא אפליקציית צרכן, לא דשבורד שיווקי.

**הימנעות מפורשת:** אין גרדיאנטים סגולים, אין צללים רכים "ידידותיים", אין אנימציות משחקיות. כל אפקט חזותי חייב לשרת הבנה.

---

## 2. Color Palette & Roles — פלטה ותפקידים

הצבע ב-Factum IL הוא **פונקציונלי**: הוא מסמן מצב, אמון ומקור — לא מותג. הפלטה נגזרת מהדיאגרמה הקיימת (כחול-לילה עמוק, ציאן טכני, כתום-ענבר לאזהרה).

### Light theme (ברירת מחדל)
```
--bg-canvas:      #F7F9FB   /* רקע ראשי, אזור קריאה */
--bg-surface:     #FFFFFF   /* כרטיסים, פאנלים, מסמכים */
--bg-subtle:      #EEF3F8   /* שורות מתחלפות, אזורים משניים */
--border:         #C9D3DD   /* קווי הפרדה, מסגרות */
--border-strong:  #9AAAB8   /* הפרדה מודגשת */

--text-primary:   #0B2545   /* טקסט ראשי, כותרות */
--text-body:      #2A3A4D   /* גוף טקסט */
--text-muted:     #5A6470   /* מטא, תוויות, עזר */

--brand:          #1B5E97   /* כחול ראשי — פעולות, קישורים */
--brand-deep:     #0B2545   /* כחול-לילה — כותרות, ניווט */
--accent:         #2E9BD6   /* ציאן טכני — הדגשה, מצב פעיל */
```

### Semantic — צבעי משמעות (חוצי theme, קריטי במערכת משפטית)
```
--valid:          #2E8B57   /* נתון מאומת / Valid Data */
--valid-bg:       #E6F4EC
--warn:           #D2691E   /* אמון נמוך / Low Confidence — דורש סקירה */
--warn-bg:        #FBEEE2
--danger:         #C0392B   /* כשל / סתירה / חריגה */
--danger-bg:      #FBEAE8
--grounded:       #1B5E97   /* טענה מעוגנת במקור */
--unverified:     #8A6D3B   /* טענה שלא אומתה — תמיד נראית שונה */
```

### Dark theme (מרכז פעולות / לילה)
```
--bg-canvas:      #0A1422
--bg-surface:     #0F1D30
--bg-subtle:      #16263D
--border:         #243650
--text-primary:   #EAF1F8
--text-body:      #C2CFDD
--text-muted:     #8294A6
--brand:          #3FA7E0
--accent:         #2E9BD6
```

**כלל זהב:** `--warn` ו-`--unverified` חייבים **תמיד** להיראות שונה מטקסט רגיל. במערכת משפטית, ההבחנה בין "מאומת" ל"לא מאומת" היא לא נואנס עיצובי — היא הליבה. לעולם אל תסתמך על צבע בלבד; צרף תמיד אייקון או תווית.

---

## 3. Typography Rules — טיפוגרפיה

המערכת דו-לשונית (עברית RTL + מונחים טכניים לטיניים). הבחירה נשלטת על-ידי **קריאוּת ממושכת**, לא אופי.

```
--font-display:  "Frank Ruhl Libre", "Heebo", serif;   /* כותרות עבריות — סמכות משפטית */
--font-ui:        "Heebo", "Assistant", sans-serif;      /* ממשק, גוף, תוויות */
--font-mono:      "IBM Plex Mono", "Menlo", monospace;   /* מזהי תיק, ציטוטים, קוד, מספרי סעיף */
```

**היררכיה (RTL — מיושר לימין):**
| רמה | גודל | משקל | font | שימוש |
|---|---|---|---|---|
| H1 | 30px | 700 | display | כותרת מסך / תיק |
| H2 | 24px | 700 | display | כותרת פאנל / חטיבה |
| H3 | 19px | 600 | ui | כותרת משנה |
| Body | 16px | 400 | ui | גוף, פסקאות מסמך |
| Label | 13px | 600 | ui | תוויות שדה, מטא |
| Mono | 14px | 500 | mono | מזהה תיק, מספר סעיף, ציטוט מקור |

**כללים:**
- מזהי תיק, מספרי סעיף וציטוטי מקור **תמיד** ב-mono — זה מסמן ויזואלית "זהו נתון מדויק שניתן לאמת".
- גובה שורה לגוף טקסט: 1.7 (קריאת מסמכים ארוכים).
- מספרים תמיד tabular-nums ליישור בטבלאות.
- כיוון: `dir="rtl"` ברמת המסמך; בלוקים לטיניים (קוד, מזהים) עוטפים ב-`dir="ltr"` נקודתי.

---

## 4. Component Stylings — סגנון רכיבים

### Cards / Panels
רקע `--bg-surface`, מסגרת `1px solid --border`, פינות `8px`, padding `20px`. ללא צל בברירת מחדל — ההפרדה היא הקו, לא הצל. צל עדין (`--elev-1`) רק לאלמנטים צפים (תפריטים, dialogs).

### Buttons
- **Primary:** רקע `--brand`, טקסט לבן, פינות `6px`, padding `10px 18px`, משקל 600.
- **Secondary:** רקע שקוף, מסגרת `--border-strong`, טקסט `--brand`.
- **Danger:** רק לפעולות הרסניות; `--danger` עם אישור מפורש.
- מצב `:focus` תמיד נראה — `outline: 2px solid --accent` (נגישות + עבודת מקלדת).

### Confidence / Source badges — הרכיב החשוב ביותר
תג קומפקטי הצמוד לכל טענה שה-AI מפיק:
- **Valid:** `--valid-bg` רקע, `--valid` טקסט, אייקון ✓, תווית "מאומת".
- **Low confidence:** `--warn-bg` רקע, `--warn` טקסט, אייקון ⚠, תווית "לסקירה".
- **Grounded:** מציג מספר מקור ב-mono, לחיץ → קופץ למקור.
- **Unverified:** `--unverified`, קו תחתון מקווקו — בלתי ניתן להחמצה.

זהו ה-DNA של Factum: כל פלט AI נושא תג מקור. רכיב זה לעולם אינו אופציונלי.

### Tables / Evidence grids
שורת כותרת `--brand-deep` עם טקסט לבן, שורות מתחלפות `--bg-subtle`, גבול `--border`. מספרים ב-tabular mono. עמודת מקור קבועה בכל טבלת ראיות.

### Inputs / Forms
מסגרת `--border`, רקע `--bg-surface`, פינות `6px`. תווית מעל השדה (לא placeholder בלבד). שגיאה → מסגרת `--danger` + הודעה מתחת, לעולם לא צבע בלבד.

### Status feedback
Toast/banner נשלט בצבע semantic; אזהרות אמון-נמוך מובילות תמיד ל-User Review Queue.

---

## 5. Layout Principles — עקרונות פריסה

- **גריד 12 עמודות**, מרווח `24px`, רוחב תוכן מרבי `1280px` למסכי עבודה.
- **RTL-first:** הניווט הראשי בימין, זרימת תוכן ימין→שמאל. כל `margin/padding` משתמש ב-`inline-start/inline-end` (לוגי), לא `left/right`.
- **שלוש אזורים קבועים בעבודת תיק:** ניווט/רשימת תיקים (ימין צר) · אזור מסמך מרכזי (רחב) · פאנל ראיות/מקורות (שמאל). הפאנל השמאלי הוא ה-grounding — תמיד נוכח.
- **צפיפות מבוקרת:** מערכת מקצועית מציגה הרבה מידע. עדיף צפיפות קריאה על-פני אווריריות מבזבזת. מרווח פנימי עקבי `16px`/`20px`.
- **Scannability:** כותרות ברורות, יישור עקבי, אזורים מופרדים בקו. עין העו"ד צריכה למצוא מידע במהירות.

---

## 6. Depth & Elevation — עומק והגבהה

עומק מינימלי ופונקציונלי. שכבות:
```
--elev-0: none;                                   /* משטח על קנבס — קו בלבד */
--elev-1: 0 1px 3px rgba(11,37,69,.08);           /* כרטיס מורם, תפריט */
--elev-2: 0 4px 12px rgba(11,37,69,.12);          /* dialog, popover */
--elev-3: 0 12px 32px rgba(11,37,69,.18);         /* modal מרכזי */
```
הפרדה ראשית = **קו** (`--border`), לא צל. צל שמור לצף. ב-dark theme הצללים עמוקים יותר אך עדינים. אין neumorphism, אין זוהר.

---

## 7. Do's and Don'ts — עשה ואל תעשה

**עשה:**
- סמן כל פלט AI בתג מקור/אמון — תמיד.
- השתמש ב-mono למזהים, סעיפים וציטוטים.
- שמור על RTL לוגי מלא; בדוק כל מסך בעברית אמיתית.
- צרף אייקון + תווית לכל סימון צבע (נגישות).
- העדף קו על צל להפרדה.

**אל תעשה:**
- אל תציג טענת AI בלי לסמן את מצב הביסוס שלה.
- אל תשתמש בגרדיאנטים דקורטיביים, צבעי מותג שמחים, או אנימציות משחקיות.
- אל תסמוך על צבע בלבד להעברת מצב קריטי.
- אל תשבור את ה-RTL עם `left/right` קשיחים.
- אל תעצב כמו אפליקציית צרכן — זו מערכת מקצועית.

---

## 8. Responsive Behavior — התנהגות רספונסיבית

המערכת היא **desktop-first** (עבודה משפטית רצינית מתבצעת על מסך גדול).
- `≥1280px`: פריסת שלושת האזורים המלאה.
- `768–1279px`: פאנל הראיות מתקפל לטאב/מגירה נשלפת; אזור המסמך מתרחב.
- `<768px`: תצוגה לקריאה והתראות בלבד — סטאק אנכי, ניווט במגירה. עריכת תיק מלאה אינה יעד מובייל.
- טבלאות רחבות → גלילה אופקית עם עמודה ראשונה (מזהה/מקור) מוקפאת.

---

## 9. Agent Prompt Guide — מדריך לסוכן

כשמבקשים ממך לבנות מסך ב-Factum IL:
1. **קבע theme:** בהיר לקריאה/עריכה, כהה למרכז פעולות.
2. **התחל מ-RTL לוגי** — `dir="rtl"`, properties לוגיות, עברית אמיתית בכל טקסט.
3. **לכל פלט AI — תג מקור.** אם אתה מציג טענה שמקורה במודל, היא חייבת badge (valid/low/grounded/unverified). זה אינו אופציונלי.
4. **mono לנתונים מדויקים** — מזהי תיק, סעיפים, ציטוטים.
5. **קו לפני צל** — הפרדה במסגרת, צל רק לצף.
6. **צבע = משמעות** — אל תשתמש בצבעי semantic לקישוט; אל תשתמש בצבע בלי אייקון+תווית.
7. **צפיפות קריאה** — זו מערכת מקצועית; אל תאוורר על חשבון יעילות.

**משפט מכונן:** *"כל פיקסל משרת הבנה או אמון. אם אינו עושה אחד מהשניים — הסר אותו."*
