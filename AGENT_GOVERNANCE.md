# מדריך הטמעה — מערכת ממשל הסוכנים

> מדריך אחד שמסביר כיצד כל הרכיבים שבנינו מתחברים לכלל מערכת ממשל אחת, וכיצד להטמיע אותם בכל פרויקט. קרא קובץ זה ראשון.

---

## התמונה הגדולה

בנינו שלוש שכבות ממשל שעובדות יחד, כולן על אותו עיקרון: מסמך מקור אחד שמחזיק טוקן, כלל, וה"למה" מאחוריו, וניתן לאכוף ולבדוק. השכבה הראשונה היא ממשל חזותי — קובצי DESIGN.md מגדירים כיצד כל מסך נראה. השכבה השנייה היא ממשל התנהגותי — קובצי AGENT.md מגדירים כיצד כל סוכן מתנהג. השכבה השלישית היא ממשל תיאום — שכבת הרב-סוכנים מגדירה כיצד סוכנים עובדים יחד. הסקיל agent-architect שומר את כל השיטה ומאפשר לזמן אותה בכל פרויקט עתידי.

## מבנה הקבצים בכל פרויקט

כל פרויקט מקבל את אותו מבנה. בשורש הפרויקט יושב קובץ `AGENTS.md` — אינדקס מרכזי שמרשם את כל הסוכנים. לצידו `AGENT.template.md` — התבנית הריקה ליצירת סוכן חדש, ו-`DESIGN.md` — שפת הממשק. בתיקיית `agents/` יושב מפרט נפרד לכל סוכן (`<name>.AGENT.md`). הסקיל `agent-architect` מותקן בתיקיית הסקילז ומשרת את כל הפרויקטים.

```
.
├── AGENT_GOVERNANCE.md        # מדריך זה — נקודת הכניסה
├── AGENTS.md                  # מרשם הסוכנים של הפרויקט
├── AGENT.template.md          # תבנית ריקה ליצירת סוכן חדש
├── DESIGN.md                  # שפת הממשק (ממשל חזותי)
├── agents/                    # מפרט נפרד לכל סוכן
│   └── <name>.AGENT.md
└── .claude/skills/agent-architect/SKILL.md   # הסקיל ששומר את השיטה
```

## הטמעה ב-Factum IL

Factum כבר עשירה בסוכנים. ההטמעה: הנח את `AGENTS.factum.md` בשורש (כ-AGENTS.md), את שלושת מפרטי הסוכנים (Summarizer, Timeline-Builder, Contract-Review) בתיקיית agents/, ואת `AGENT.template.md` לצידם. ה-Orchestrator הקיים מתעדכן לטעון את מפרט הסוכן הרלוונטי בעת זימון, ולאכוף את מבנה הפלט. שפת הממשק `DESIGN.factum.md` מנחה את React 19 Dashboard. הצעד הבא הטבעי: מימוש Verification-Agent מתוכנית V14, שכבר מתועד בתבנית כדוגמה.

> במאגר זה (`factum-il-stage-a`) ההוכחה הרצה כבר קיימת תחת `stage-a/` — מנהל, עובד וזיכרון משותף, עם חמשת מנגנוני הממשל נאכפים בקוד. ספריית הסוכנים שמתווספת כאן מתעדת את החוזים שאותה הוכחה כבר ממשת.

## הטמעה ב-Leasing.co.il

Leasing מתחילה ספריית סוכנים מאפס — וזו הזדמנות לבנות אותה נכון מהיום הראשון. ההטמעה: הנח את `AGENTS.leasing.md` בשורש (כ-AGENTS.md), את שני המפרטים המסחריים (Deal-Scorer, Finance-Simulator) בתיקיית agents/, ואת התבנית לצידם. שפת הממשק `DESIGN.leasing.md` מנחה את הפלטפורמה. הסוכנים נכתבו בפרופיל המסחרי — שקיפות מלאה, אמון לפני דחיפה, ואישור אנושי לכל התחייבות. הצעד הבא: הרחבת הספרייה ל-Vehicle-Matcher ו-Supplier-Sync.

## כיצד ליצור סוכן חדש בכל פרויקט

בכל פרויקט, יצירת סוכן חדש היא תהליך אחיד. הפעל את הסקיל agent-architect (אמור "תגדיר לי סוכן ש..."), בחר את הפרופיל לפי הפרויקט (משפטי ל-Factum, מסחרי ל-Leasing), ומלא את שבעת המקטעים. ודא שכל עקרונות-העל מיושמים — במיוחד שדה ה-evidence[]. הוסף את הסוכן החדש למרשם ב-AGENTS.md. בכך הספרייה נשארת עקבית ומתועדת תמיד.

## כיצד להפעיל שכבה רב-סוכנית

כאשר משימה דורשת תיאום בין כמה סוכנים, אל תבנה סוכן-על. הפעל את שכבת הרב-סוכנים: מנהל שמתכנן ומנתב, עובדים (הסוכנים הקיימים) שמבצעים, סוקר שמבקר, וזיכרון משותף שמחבר. הממשל מונע כאוס: מנהל יחיד, תקרת צעדים, שער אנושי, ויומן מלא. התחל משלב א' — מנהל אחד ועובד אחד — כהוכחת היתכנות בסיכון מינימלי. אותו מבנה עובד בשני הפרויקטים.

## עקרון מנחה אחד

כל הרכיבים חולקים עיקרון אחד: ממשל הופך החלטות מ"תחושה בזמן הבנייה" ל"מסמך מקור אחד שניתן לאכוף ולבדוק". זה ההבדל בין מערכת שגדלה בכאוס למערכת שגדלה בשליטה — והמעבר מ-AI כצ'אטבוט ל-AI כמערכת הפעלה.
